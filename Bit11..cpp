#include <bits/stdc++.h>
using namespace std;

int main (){
   int test;
   scanf ("%d", &test);
   while (test--){
        int N;
        scanf ("%d", &N);
        int arr[N];
        for (int i = 0; i<N; i++) scanf ("%d", &arr[i]);
        printf ("0\n");
   }
   return 0;
}
